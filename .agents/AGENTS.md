# Workspace Rules

- **Git Push Policy**: Do NOT perform automatic `git push` on every single small code edit. Work and verify locally on the dev server. Commit/push to GitHub only when explicitly requested by the user, or after completing a major milestone.
